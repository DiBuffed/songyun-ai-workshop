# Songyun AI Workshop - Python dependencies
# Install with: pip install -r requirements.txt

# Core ML
torch>=2.0.0
diffusers>=0.25.0
transformers>=4.35.0
accelerate>=0.24.0

# UI
gradio>=4.0.0

# Config
python-dotenv>=1.0.0

# For loading safetensors LoRA files (included in diffusers, but explicit for clarity)
safetensors>=0.4.0
